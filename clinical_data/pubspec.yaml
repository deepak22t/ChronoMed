name: clinical_data
description: Offline clinical drug database and pharmacokinetic rule extractor for ChronoMed.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  core_engine:
    path: ../core_engine

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
